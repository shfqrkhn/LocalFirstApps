# Capability, Recovery, And Test Matrix

Last verified: 2026-07-21. Status terms: **PASS** is covered by a local automated gate; **PASS_WITH_LIMITATIONS** is intentional product scope or partial recovery; **N/A** means the capability is not part of the product contract.

| App | Primary data / storage | Export and import | Reset / corrupt-state recovery | Local-file contract | Single-input evidence | Current automated evidence |
| --- | --- | --- | --- | --- | --- | --- |
| TS-Dash | CSV-derived time-series data in IndexedDB | PASS: normalized CSV, dataset JSON, chart PNG, and view summary exports; CSV and dataset JSON imports | PASS_WITH_LIMITATIONS: guarded dataset/all-data deletion; source CSV or exported dataset is the recovery source; no automatic repair of arbitrary IndexedDB corruption | PASS_WITH_LIMITATIONS: safe launcher notice; hosted origin required for the app runtime | PASS: keyboard analysis shortcuts plus pointer/touch import controls | Static, five responsive widths, file fallback, valid CSV import, normalized export, guarded clear |
| PMQuiz | Active quiz in memory; theme preference in localStorage | PASS_WITH_LIMITATIONS: custom question JSON import; progress export/import is intentionally absent | PASS_WITH_LIMITATIONS: guarded current-session reset; malformed custom files fail closed; no progress recovery after closing/reset | PASS_WITH_LIMITATIONS: safe launcher notice; hosted origin required for worker/PWA behavior | PASS: keyboard question selection/answering and pointer/touch reset | Static, responsive, file fallback, malformed JSON rejection, keyboard quiz, guarded reset |
| Noodle Nudge | Assessment/reflection state in IndexedDB | PASS: JSON backup export and validated JSON restore (2 MB limit) | PASS: malformed/unsafe backups rejected; guarded full data reset | PASS_WITH_LIMITATIONS: safe launcher notice; hosted origin required for app data/content loading | PASS: keyboard-native controls and pointer/touch navigation/actions | Static, responsive, file fallback, export, malformed backup rejection, guarded reset |
| Flexx Files | Training state under scoped `flexx_` localStorage keys | PASS: JSON backup and validated restore | PASS: invalid imports fail closed; scoped reset preserves unrelated storage; draft/progression recovery has native tests | PASS_WITH_LIMITATIONS: safe launcher notice; hosted origin required for module/PWA runtime | PASS: labeled bottom navigation and native keyboard/pointer/touch controls | Static, responsive, file fallback, backup/corrupt restore UI, native correctness, plate math, draft, and scoped-reset tests |
| CommonGround | Facilitation and Decision Analysis matters in versioned IndexedDB with revisioned singleton records | PASS: integrity-protected matter/workspace JSON and ZIP, Markdown/print briefs, CommonGround v1, and LedgerSuite v1/v2 imports (25 MB CommonGround; 5 MB Ledger) | PASS: guided atomic/idempotent legacy migration, complete source backup, integrity repair, selected-matter deletion closure, source-preserving migration, stale-write rejection, and app-scoped backup-before-reset | PASS_WITH_LIMITATIONS: safe launcher notice; hosted origin required for module, migration, worker, PWA, and optional OPFS behavior; semantic conflict reconciliation across independent records is deferred | PASS: semantic forms and routes support keyboard, pointer, and touch without cross-mode dependencies | Static; five responsive widths; file fallback; facilitation/decision isolation; shared/personal/professional contexts; separate hard constraints and scores; integrity/ZIP corruption rejection; migration rollback/idempotency; scoped deletion/reset; first install/offline restart; user-staged worker update; stale-write rejection; redirect |

## Gate Boundaries

- `npm run test:local` is the deterministic local closure gate. It does not contact Pages.
- `npm run test:live` is a read-only deployment check and remains a separate external gate.
- File-mode tests prove a safe, usable fallback notice and return link; they do not claim that module/worker/PWA runtimes work under `file://`.
- Automated tests use synthetic local data in isolated browser contexts. They never inspect browser profiles, accounts, or personal data.
- Current automation does not certify every screen reader, browser-specific storage eviction policy, or longitudinal multi-device workflow.
